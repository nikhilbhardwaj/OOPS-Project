The visual development kit c++ library
	
the visual development kit (vdk) is a c++ library
that wraps the gtk+ toolkit.

this package contains the reference for vdk. it will be
put in /usr/share/doc/vdk-doc/.