Simple, flexible mock object library for ruby unit testing
	
flexmock is a simple, but flexible, mock object library for ruby unit
testing.

this is a dependency package which depends on debian's default ruby version
(currently 1.8).