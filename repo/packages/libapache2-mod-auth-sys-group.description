Module for apache2 which checks user against system group
	
mod_auth_pam implements 'require group' functionality against system group
database.

this package provides the module for apache 2.0 server.