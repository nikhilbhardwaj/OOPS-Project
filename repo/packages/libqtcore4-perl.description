Perl bindings for the qt core library
	
this package contains perl bindings for the qt core library.

it enables the use of qt applications written in perl. for more details
see the libqtcore4 package.